using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Purchasing;

public class NonConsumablePurchase : IAP
{
    /*
    protected override void Awake()
    {
        base.Awake();
    }
    public override void OnNonConsumablePurchaseComplete(Product product)
    {
        base.OnNonConsumablePurchaseComplete(product);
        ApplyProductID(ýAPButton.productId, product);
        Debug.Log("All Ads are removed");
    }


    protected override string ApplyProductID(string ID, Product product)
    {
        return ID = product.definition.id;
    }
    */
}
