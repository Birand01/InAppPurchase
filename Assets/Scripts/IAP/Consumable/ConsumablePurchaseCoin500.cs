using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Purchasing;
using UnityEngine.UI;

public class ConsumablePurchaseCoin500 : IAP
{

    /*
    protected override void Awake()
    {
        base.Awake();
    }
    public override void OnConsumablePurchaseCoinComplete(Product product)
    {
        base.OnConsumablePurchaseCoinComplete(product);
        //ApplyProductID(ýAPButton.productId, product);
        //Debug.Log("500 Coins are received");
    }



    protected override string ApplyProductID(string ID, Product product)
    {
        return ID = product.definition.id;
    }
    */
}
