using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Purchasing;

public class ConsumableDiamond10 : IAP
{

    /*
    protected override void Awake()
    {
        base.Awake();
    }
    public override void OnConsumablePurchaseDiamondComplete(Product product)
    {
        base.OnConsumablePurchaseDiamondComplete(product);
        ApplyProductID(ýAPButton.productId, product);
        Debug.Log("10 Diamonds are received");
    }

    protected override string ApplyProductID(string ID, Product product)
    {
        return ID = product.definition.id;
    }
    */
}
